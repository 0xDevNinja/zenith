// @zenith/sdk — public entry point.
//
// Will expose: pool/pair + position state fetch, exact-math quoting
// (mirrors on-chain math), transaction builders, and DLMM strategy helpers.
// Implementation lands in M2.

export const VERSION = "0.1.0";
