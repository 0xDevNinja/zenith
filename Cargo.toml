[workspace]
resolver = "2"
members = [
    "programs/zenith-amm",
    "programs/zenith-dlmm",
    "crates/zenith-math",
]

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
