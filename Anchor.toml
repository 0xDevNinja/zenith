[toolchain]

[features]
resolution = true
skip-lint = false

# Program IDs are placeholders — regenerate with `anchor keys sync` after
# generating program keypairs (`anchor keys list`).
[programs.devnet]
zenith_amm = "AA8cKcHQj63GEHRaLrrT87W1efRZ44U147JTCXC2Rmkq"
zenith_dlmm = "Zen1DLMMpLACEHODLERpLACEHODLERpLACEHODLER11"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
